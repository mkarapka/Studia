#include <iostream>
#include <typeinfo>

int main(){
    std::string var = "x1";
    std::cout << std::stod(var) << std::endl;
    return 0;
    // while (!costam.empty())
            // {
            //     std::cout << typeid(*(costam.front())).name() << std::endl;
            //     costam.pop();
            // }

            // for (int i = 0; i < listof_sExp.size(); i++)
            // {
            //     std::cout << kalkulator::isVariable(listof_sExp[i]) << ", ";
            // }
}