  0 1 2 3 4 5 6 7
0 # # # # # # # #
1 # # # # # # # #
2 # # 1 # # # # #
3 # # 1 0 0 # # #
4 0 0 1 0 1 # # #
5 # # 1 # # # # #
6 # # 1 # # # # #
7 # # # # # # # #


// typedef std::vector<Point, std::vector<std::string>> vec_point_vec_str;
// typedef std::pair<Point, std::vector<Point>> pair_of_point_vec_point;
// namespace std {
//     template <>
//     struct hash<Point> {
//         std::size_t operator()(const Point& p) const {
//             return std::hash<int>()(p.x) ^ (std::hash<int>()(p.y) << 1);
//         }
//     };
// }

// State board;
    // print_board(board.board);

    // board.board = board.make_move(board.board, 0, {4,2});
    // print_board(board);
    // board.board = board.make_move(board.board, 1, {4, 1});
    // print_board(board);
    // board.board = board.make_move(board.board, 0, {5, 2});
    // board.board = board.make_move(board.board, 0, {4, 0});
    // board.board = board.make_move(board.board, 1, {2, 2});
    // board.board = board.make_move(board.board, 0, {3, 2});
    // board.board = board.make_move(board.board, 1, {6, 2});
    // // print_board(board);

    // // board.board = board.make_move(board.board, 1, {5, 3});
    // print_board(board);

    // for(auto& p : board.get_legal_points(0)){
    //     std::cout << p.row << p.col << std::endl;
    // }
    //
